# 版本历史

本项目遵循语义版本。日期使用 `YYYY-MM-DD`。

## 2.3.0 - 2026-08-11

### 新增

- 服务端新增共享 AAC 到 Opus 低延迟转码链路。每路活动 RTMP 源只启动一个 FFmpeg 工作进程，所有 WebRTC 观看者复用同一份 Opus RTP。
- 单一 Docker 镜像内置按功能白名单编译的 FFmpeg；Linux 直接安装脚本会安装或检查同一运行依赖。
- 服务端内置公网 IPv4 自动发现和周期刷新。`media.public_ip` 留空时通过本机接口与可配置 STUN 服务探测地址，填写 IPv4 时使用手动覆盖值。

### 变更

- 浏览器默认不静音，优先使用 WebRTC 同时播放 H.264 视频和 Opus 音频；只有协商、连接或媒体轨道失败时才回退 HTTP-FLV。
- 浏览器阻止有声自动播放时保留 WebRTC 会话，等待用户交互后继续，不把自动播放限制误判为媒体故障。

### 修复

- WebRTC 语音和浏览器播放按 PeerConnection 读取当前公网 IPv4，动态地址变化后新连接无需重启服务即可使用新候选地址；服务端媒体候选仅使用 IPv4。
- HTTP-FLV 接管播放会话后不再自动切回 WebRTC。FLV 中断只重建 FLV 播放器，并使用有限次数的指数退避，避免两种协议无限循环切换。

## 2.2.1 - 2026-08-10

### 修复

- 修复 RTMP 源流包含 AAC 时服务端直接拒绝浏览器 WebRTC 协商的问题。WebRTC 继续播放 H.264 视频，需要源流声音时由网页切换 HTTP-FLV。
- 修复网页默认非静音导致浏览器拦截自动播放，继而让 WebRTC 和 FLV 都表现为失败的问题。
- 修复播放器主动关闭旧 PeerConnection 时误触发失败回退，以及无音频 RTP 被误判为 WebRTC 故障的问题。
- 根据源流 SPS 匹配 H.264 Baseline、Main 和 High Profile，避免 SDP 声明与实际编码不一致造成无首帧。

## 2.2.0 - 2026-08-10

### 新增

- RTMP 临时直播默认能力。合法但未绑定房间的 Stream Key 可以在推流期间出现在网页直播大厅，并可通过配置关闭。
- 临时 Stream Key 格式校验、重复发布保护和网页临时直播回归测试。

### 变更

- 临时直播不写入 SQLite、不绑定房间，推流断开后自动从网页直播大厅移除。
- 管理配置接口会返回当前临时直播开关；修改 YAML 后重启服务生效。

## 2.1.0 - 2026-07-17

### 新增

- 黑白亮色与暗色主题，以及持久化外观设置。
- 客户端可执行文件同级 data 目录。
- 账号级消息缓存隔离与数据库升级测试。
- 开源贡献、安全、行为准则和版本历史文档。

### 变更

- 重构标题栏、导航、工作面板、表单、按钮、对话框和交互状态。
- 所有组件改用 ThemeExtension 上下文令牌，同步响应主题切换。
- 网络图片改用进程内缓存，不在系统临时目录持久化账号图片。
- 语音活动检测使用进入/退出双阈值和静音保持时间；成员圆点使用常亮中心和呼吸光晕。
- 公开技术文档统一由 `docs/` 管理，并以完整的 `docs/NexusRoom.md` 记录架构、协议和开发标准。

### 修复

- 修复同一服务器不同账号读取相同本地消息缓存的问题。
- 修复切换主题后部分已挂载组件颜色不更新的问题。
- 修复短暂停顿导致说话呼吸灯频繁停止和重启的问题。
- 修复切换房间时麦克风按钮长时间处于加载状态，以及关闭窗口等待原生资源清理的问题。
- 修复清除本地数据时删除整个 data 目录并关闭数据库，导致当前进程无法继续运行的问题。

## 2.0.0 - 2026-07-15

- 服务端整合为单一 Go 进程和单容器部署。
- 使用 SQLite、内建语音 SFU、RTMP、HTTP-FLV/WebRTC 播放、TURN 和 WireGuard 协调。
- 分离客户端、服务端、部署和公开文档目录。
