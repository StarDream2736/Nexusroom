# 安全策略

## 支持范围

| 版本 | 安全更新 |
| --- | --- |
| 2.x 最新次版本 | 支持 |
| 更早版本 | 不保证 |

## 报告漏洞

请使用 GitHub Security Advisory 的私密报告功能联系维护者。不要为尚未修复的漏洞创建公开 Issue，也不要附带真实令牌、密码、私钥、数据库或个人数据。

报告应包含受影响版本、部署方式、复现条件、预期影响和可行的缓解建议。维护者会确认收到报告、评估影响，并在修复和发布准备完成后协调披露。

## 部署责任

生产部署必须替换模板中的 JWT、管理员和 TURN 凭据，保护 `config.yaml` 与 data 目录，使用 TLS 入口，并限制管理端口和 UDP 范围。发现凭据泄露时应立即轮换，不要只删除 Git 历史中的单个文件。
